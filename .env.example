# 环境变量配置示例
# 复制此文件为 .env 并填入您的实际配置值

# 会话密钥（用于 JWT 签名）
SESSION_SECRET=your_secure_random_string_here

# 腾讯云访问密钥
# 在腾讯云控制台 > 访问管理 > API密钥管理 中获取
TENCENT_SECRET_ID=your_secret_id_here
TENCENT_SECRET_KEY=your_secret_key_here

# COS 存储桶配置
# 在腾讯云 COS 控制台中获取
COS_REGION=ap-guangzhou
COS_BUCKET=your-bucket-name

# CDN 配置
CDN_ENABLED=true
CDN_DOMAIN=your-cdn-domain.com

# CDN 鉴权配置
# 主鉴权密钥 (6-40位大小写字母、数字)
CDN_AUTH_KEY_PRIMARY=your-cdn-auth-key-primary

# 备鉴权密钥 (6-40位大小写字母、数字)  
CDN_AUTH_KEY_BACKUP=your-cdn-auth-key-backup

# 签名有效时间 (秒)
CDN_AUTH_TTL=10

# 签名参数名
CDN_AUTH_PARAM=sign

# 示例配置说明：
# SESSION_SECRET: 至少32位的随机字符串，用于加密用户会话
# TENCENT_SECRET_ID: 腾讯云账户的 SecretId
# TENCENT_SECRET_KEY: 腾讯云账户的 SecretKey  
# COS_REGION: COS 存储桶所在区域，如 ap-guangzhou、ap-shanghai 等
# COS_BUCKET: COS 存储桶名称，不包含 appid
# CDN_DOMAIN: CDN 加速域名，不含 https:// 前缀
# CDN_AUTH_KEY_PRIMARY: CDN 主鉴权密钥
# CDN_AUTH_KEY_BACKUP: CDN 备用鉴权密钥
# CDN_AUTH_TTL: 签名有效期（秒），推荐 10-3600 秒
# CDN_AUTH_PARAM: 签名参数名，通常为 'sign'

SQLITE_PATH=./data.sqlite

ALLOW_REGISTER=true


#MySQL 配置(如有)
#DATABASE_URL=mysql://username:password@host:port/database

#你的网站是否为HTTPS（涉及到cookie选项）
HTTPS=true